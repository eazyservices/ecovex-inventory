Drop your logo file here as "logo.png" (or logo.svg — then update the
<img src> in each page's header from logo.png to logo.svg).

Recommended: a roughly square or wide logo, transparent background,
at least 200px tall so it stays sharp. It'll show at 40px tall in the
header automatically — no code changes needed once the file is here.
